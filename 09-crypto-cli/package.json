{
  "name": "cryptocli",
  "version": "1.0.0",
  "description": "This project demonstrates how to build a `Command Line Interface (CLI)` with `Node.js`.",
  "main": "index.js",
  "bin": {
    "cryptocli": "./build/src/cli/cryptocli.js"
  },
  "scripts": {
    "clean": "del-cli ./build/*",
    "build": "npm run clean && tsc",
    "build:watch": "npm run clean && tsc --watch",
    "install:cli:windows": "npm link",
    "install:cli:mac": "sudo npm link"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/configstore": "^5.0.1",
    "@types/inquirer": "^8.1.1",
    "@types/node": "^16.9.2",
    "del-cli": "^4.0.1",
    "typescript": "^4.4.3"
  },
  "dependencies": {
    "colors": "^1.4.0",
    "commander": "^8.2.0",
    "configstore": "^6.0.0",
    "inquirer": "^8.1.5",
    "node-fetch": "^3.0.0"
  }
}
